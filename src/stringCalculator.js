function add(numbers) {
  // With empty string
    if (numbers === "") {
        return 0;
    }

  // With a zero
   if (numbers === "0") {
        return 0;
    }
}

module.exports = {
    add,
};

